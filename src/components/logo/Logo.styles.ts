import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  text: {
    fontSize: 25,
    width: '100%',
    textAlign: 'center',
    marginBottom: '5%',
    fontWeight: 'bold',
  },
  logoContainer: {
    alignItems: 'center',
  },
});
