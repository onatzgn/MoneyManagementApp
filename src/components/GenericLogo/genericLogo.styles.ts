import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    text: {
        fontSize: 25,
        fontWeight:'bold',
        width: '100%',
        textAlign: 'center',
    },
    logoContainer: {
        alignItems: 'center'
    }
})