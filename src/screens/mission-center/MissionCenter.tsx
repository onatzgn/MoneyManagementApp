import React from 'react';
import {SafeAreaView} from 'react-native';
import {styles} from './MissionCenter.style';
import {Text, Logo} from '@components';

export const MissionCenter = () => {
  return (
    <SafeAreaView>
      <Text text="MissionCenter"></Text>
    </SafeAreaView>
  );
};
