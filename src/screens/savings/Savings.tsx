import React from 'react';
import {SafeAreaView} from 'react-native';
import {styles} from './Savings.style';
import {Text, Logo} from 'components/Index';

export const Savings = () => {
  return (
    <SafeAreaView>
      <Text text="Savings"></Text>
    </SafeAreaView>
  );
};
